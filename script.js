//your JS code here. If required.
// 1. Use the location object to get the current URL
const currentURL = window.location.href;

// 2. Use the length property to count the letters
const urlLength = currentURL.length;

// 3. Use alert() to print the final length in the specific format
alert("The length of the URL is: " + urlLength);